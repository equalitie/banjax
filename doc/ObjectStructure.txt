* Event handler class:
** All handles,
** creation and the destruction of the contuaitions.
* Error generator class:
** Generates Error pages.
* Regex Class.
** Read the regexes.
** It should be able to re-read regex without the need to restart ATS.
*** I suggest some kind of authenticated get request telling ats to re-read the central Banning DB.
** Does the search regex.
